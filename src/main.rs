use gj::experiments::*;
// use gj::util::*;

fn main() {
    //lj_sum(68993773);
    community(100000, 0.7, 0.1);

    //let args: Vec<String> = std::env::args().collect();
    //let n: usize = args[1].parse().unwrap();

    //live_journal(68993773);
}
